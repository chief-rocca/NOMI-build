export const CONFIG = {
    // Toggle this to enable "Fast Login" button for development speed
    IS_DEV_MODE: true,

    // Simulated Networking Config
    MIN_LATENCY_MS: 600,
    MAX_LATENCY_MS: 1500,
};
